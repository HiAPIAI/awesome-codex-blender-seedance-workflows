# Never commit a real key. The CLI does not load this file automatically.
HIAPI_API_KEY=
# Keep the official origin unless you have reviewed a custom endpoint.
HIAPI_BASE_URL=https://api.hiapi.ai
# Optional absolute path when Blender is not on PATH.
BLENDER_BIN=
# Optional absolute paths when FFmpeg and ffprobe are not on PATH.
FFMPEG_BIN=
FFPROBE_BIN=
